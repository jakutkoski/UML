'use strict';

/**
 * @ngdoc function
 * @name sinpfApp.controller:InstructionsCtrl
 * @description
 * # InstructionsCtrl
 * Controller of the sinpfApp.
 * Currently doesn't do anything, but left here if we want to add more later.
 */
angular.module('sinpfApp').controller('InstructionsCtrl', function () {
  this.isStatic = true;
});
